# The Hyperswitch SDK depends on rescript, whose postinstall spawns a bundled
# ninja binary that fails on Windows hosts. Nothing in this project compiles
# ReScript, so lifecycle scripts are unnecessary and skipping them makes
# installs behave identically on Windows, Linux, and CI.
# See docs/DECISIONS.md D-001.
ignore-scripts=true
